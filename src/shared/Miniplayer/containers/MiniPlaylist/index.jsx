import React from 'react';

import './index.css';

const MiniPlaylist = ({ display }) => {
  return (
    <div className="MiniPlaylist" style={{ display }}>
      <span>Render here the MiniPlaylist</span>
    </div>
  );
};

export default MiniPlaylist;
