import React from 'react';

import './index.css';

const Buttons = () => {
  return (
    <div className="Header-buttons">
      <button>Login</button>
      <button>Signin</button>
    </div>
  );
};

export default Buttons;
