import React from 'react';

import './index.css';

const FilterBox = ({ items }) => {
  return (
    <ul className="FilterBox">
      <span>FilterBox</span>
    </ul>
  );
};

export default FilterBox;
